import React, { useMemo, useState } from 'react'
import { Card, PageHead, Badge, formatWon } from '../components.jsx'

export default function PaymentsHistory({data, updatePayment, cancelPayment, resetAllPayments, navigate}){const [q,setQ]=useState(''); const [edit,setEdit]=useState(null); const rows=useMemo(()=>[...data.payments].filter(p=>!q||[p.name,p.vehicleNo,p.method,p.paidForYm,p.sigun,p.chargeItem].join(' ').includes(q)),[data.payments,q]); const total=rows.reduce((s,p)=>s+(Number(p.amount)||0),0); const month='2026-06'; const monthTotal=rows.filter(p=>(p.paidDate||'').startsWith(month)).reduce((s,p)=>s+(Number(p.amount)||0),0); const bank=rows.filter(p=>p.method==='통장매칭').length; const direct=rows.filter(p=>p.method!=='통장매칭').length; return <div><PageHead title="수납내역" desc="직접 수납·통장매칭·CMS 수납 반영 이력입니다."/>
  <div className="grid grid-4" style={{marginBottom:14}}><Card className="stat"><div className="stat-label">총 수납 건수</div><div className="stat-value">{rows.length}건</div></Card><Card className="stat"><div className="stat-label">총 수납액</div><div className="stat-value">{formatWon(total)}</div></Card><Card className="stat"><div className="stat-label">이번 달 ({month})</div><div className="stat-value">{formatWon(monthTotal)}</div></Card><Card className="stat"><div className="stat-label">통장/직접</div><div className="stat-value">{bank} / {direct}건</div></Card></div>
  <Card className="card-pad" style={{marginBottom:14}}><div className="filters"><input className="input" value={q} onChange={e=>setQ(e.target.value)} placeholder="이름 / 차량번호 / 지역 / 방식 / 대상월 검색"/><button className="btn soft">CSV 내보내기</button><button className="btn red" onClick={()=>{if(confirm('전체 수납내역을 초기화하고 협회비/관리비 차감분을 미수금으로 복구할까요?\n통장매칭으로 생성된 수납도 초기화됩니다.')) resetAllPayments?.()}}>수납내역 전체 초기화</button></div></Card><Card><div className="table-wrap"><table className="table"><thead><tr><th>수납일</th><th>이름</th><th>차량번호</th><th>지역</th><th>대상월</th><th>항목</th><th>방식</th><th className="right">금액</th><th>처리일</th><th>메모</th><th className="right">처리</th></tr></thead><tbody>{rows.map(p=><tr key={p.id}><td>{p.paidDate}</td><td><button type="button" className="name-link admin-name" onClick={()=>navigate?.('list',{q:p.vehicleNo || p.name, memberId:p.memberId || p.member_id, amount:'전체', status:'정상', openPayments:true})}><b>{p.name}</b></button></td><td className="mono">{p.vehicleNo}</td><td>{p.sigun || p.region}</td><td>{p.paidForYm}</td><td>{p.chargeItem}</td><td><Badge tone={p.method==='통장매칭'?'blue':'gray'}>{p.method}</Badge></td><td className="right money">{formatWon(p.amount)}</td><td>{(p.createdAt||'').slice(0,10)||'-'}</td><td className="ellipsis">{p.memo}</td><td className="right action-cell"><button className="btn soft" onClick={()=>navigate?.('list',{q:p.vehicleNo || p.name, memberId:p.memberId || p.member_id, amount:'전체', status:'정상', openPayments:true})}>회원보기</button><button className="btn" onClick={()=>setEdit(p)}>수정</button><button className="btn red" onClick={()=>{if(confirm('이 수납을 취소하고 미수금액을 복구할까요?')) cancelPayment(p.id)}}>취소</button></td></tr>)}{!rows.length&&<tr><td colSpan="11" style={{textAlign:'center',padding:40,color:'var(--sub)'}}>수납 내역이 없습니다. 미수금명단에서 수납 처리 후 이력이 표시됩니다.</td></tr>}</tbody></table></div></Card>{edit&&<PaymentEditModal item={edit} onClose={()=>setEdit(null)} onSave={(payload)=>{updatePayment(edit.id,payload); setEdit(null)}}/>}</div>}
function PaymentEditModal({item,onClose,onSave}){const [amount,setAmount]=useState(item.amount||0); const [method,setMethod]=useState(item.method||'직접수납'); const [paidDate,setPaidDate]=useState(item.paidDate||''); return <div className="modal-bg"><div className="modal"><h3>수납내역 수정</h3><div className="form-row"><b>대상</b><span>{item.name} / {item.vehicleNo}</span></div><div className="form-row"><b>수납일</b><input className="input" type="date" value={paidDate} onChange={e=>setPaidDate(e.target.value)}/></div><div className="form-row"><b>수납방법</b><select className="select" value={method} onChange={e=>setMethod(e.target.value)}><option>직접수납</option><option>통장매칭</option><option>CMS</option><option>현금</option></select></div><div className="form-row"><b>금액</b><input className="input" type="number" value={amount} onChange={e=>setAmount(e.target.value)}/></div><div className="notice">금액 수정은 수납 기록만 변경합니다. 이미 차감된 미수금 복구는 “취소” 기능을 사용하세요.</div><div style={{display:'flex',justifyContent:'flex-end',gap:8,marginTop:16}}><button className="btn" onClick={onClose}>취소</button><button className="btn primary" onClick={()=>onSave({amount:Number(amount)||0, method, paid_date:paidDate})}>저장</button></div></div></div>}
